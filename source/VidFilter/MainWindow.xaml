<Window x:Class="VidFilter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VidFilter"
        Title="VidFilter Home" Height="Auto" Width="Auto" MinHeight="250" MinWidth="600" Name="HomeWindow"
        Closing="HomeWindow_Closing">
    <StackPanel Orientation="Vertical" Height="Auto">
        <TabControl>
            <TabItem Header="Filter" Name="FilterTabItem">
                <Border Style="{StaticResource BorderStyle}">
                    <local:FilterGrid/>
                </Border>
            </TabItem>
            <TabItem Header="Admin" Name="AdminTabItem">
                <Border Style="{StaticResource BorderStyle}">
                    <local:AdminPanel/>
                </Border>
            </TabItem>
        </TabControl>
        <TextBox Name="DebugInformation" Text="{Binding Path=DebugInformation,Mode=OneWay}" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" IsReadOnly="True" Height="122" />
    </StackPanel>
</Window>
